<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EcoSentinel.WeatherSensorPage"
             xmlns:local="clr-namespace:EcoSentinel.View"
             >

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/> 
                <RowDefinition Height="*"/>  
                <RowDefinition Height="*"/>  
                <RowDefinition Height="20"/> 
            </Grid.RowDefinitions>
        
            <Grid Grid.Row="0">
                <local:HeaderPage/>
            </Grid>
            
            <Grid Grid.Row="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border 
                    x:Name="mapFrame"
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="5"
                    Padding="0">

                    <WebView Source="https://maps.googleapis.com/maps/api/staticmap?center=55.008785,-3.5856323&amp;zoom=14&amp;size=600x400&amp;markers=size:mid|color:red|55.008785,-3.5856323&amp;key=AIzaSyDZ_IPjUa_8adkSX8-In_yNEGtcmBYqLVk" 
                             Margin="0"
                             HorizontalOptions="Fill"
                             VerticalOptions="Fill"/>

                </Border>

            <Border x:Name="nodeFrame"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="5"
                    Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Text="Sensor List" Padding="8" Background="LightGray" />

                    <ListView Grid.Row="1" Grid.RowSpan="3"
                              ItemsSource="{Binding SensorData}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Frame Padding="8" Margin="4" >
                                        <Label Text="{Binding siteName}" FontSize="18"/>
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding BindingContext.NavigateCommand, Source={x:Reference nodeFrame}}"  
                                                CommandParameter="{Binding .}" />
                                        </Frame.GestureRecognizers>
                                    </Frame>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
        
            <Grid Grid.Row="2">           
                <Border x:Name="sensorDataFrame"
                        Grid.Row="0"
                        Grid.Column="0" 
                        Margin="5">
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>                       
                        </Grid.RowDefinitions>
                    
                        <Label Grid.Row="0" Text="Sensor Data" Padding="8" Background="LightGray" />
                        <Label Grid.Row="1" Text="ENTRY" Padding="8" />
                        <Label Grid.Row="2" Text="ENTRY" Padding="8" />
                        <Label Grid.Row="3" Text="ENTRY" Padding="8" />
                        <Label Grid.Row="4" Text="ENTRY" Padding="8" />                   
                    </Grid>
                </Border>
            </Grid>
            
            <Grid Grid.Row="3">
                <local:FooterPage/>
            </Grid>
            
        </Grid> 
    
</ContentPage>